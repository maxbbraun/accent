[build-system]
requires = ['setuptools==83.0.0', 'wheel==0.47.0', 'numpy==2.4.4']
build-backend = 'setuptools.build_meta'

[project]
name = 'dithering'
version = '1.0.0'
description = 'Floyd-Steinberg dithering written in C'
dependencies = ['numpy==2.4.4']
